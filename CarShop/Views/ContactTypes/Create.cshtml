@model TecDocEcoSystemDbClassLibrary.ContactType
@using CarShop.Properties;
@{
    ViewBag.Title = Resources.CREATENEW_ACTION;
}

<h2>@Resources.CREATENEW_ACTION <small>@Resources.ContactType_INDEX</small></h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        @*<h4>ContactType</h4>*@
        <ol class="breadcrumb">
            <li>@Html.ActionLink(Resources.ContactType_INDEX, "Index")</li>
        </ol>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.ContactTypeId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ContactTypeId)
                @Html.ValidationMessageFor(model => model.ContactTypeId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ContactTypeDescription, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.ContactTypeDescription)
                @Html.ValidationMessageFor(model => model.ContactTypeDescription)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="@Resources.CREATE_ACTION" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink(Resources.BACKTOLIST_ACTION, "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
