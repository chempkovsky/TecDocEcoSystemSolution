@model TecDocEcoSystemDbClassLibrary.StreetType
@using CarShop.Properties;
@{
    ViewBag.Title = Resources.DETAILS_ACTION;
}

<h2>@Resources.DETAILS_ACTION</h2>

<div>
    @*<h4>StreetType</h4>*@
    <ol class="breadcrumb">
        <li>@Html.ActionLink(Resources.StreetType_INDEX, "Index")</li>
    </ol>
	<hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StreetTypeId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StreetTypeId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StreetTypeDescription)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StreetTypeDescription)
        </dd>

    </dl>
</div>
<p>
    @if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
    {
        @Html.ActionLink(Resources.EDIT_ACTION, "Edit", new { id = Model.StreetTypeId }, new { @class = "btn btn-primary" })
    }
    @Html.ActionLink(Resources.BACKTOLIST_ACTION, "Index")
</p>
