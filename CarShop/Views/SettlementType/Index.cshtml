@model IEnumerable<TecDocEcoSystemDbClassLibrary.SettlementType>
@using CarShop.Properties;
@{
    ViewBag.Title = Resources.SettlementType_INDEX;
}

<h2>@Resources.SettlementType_INDEX</h2>

@if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
{
    <p>
        @Html.ActionLink(Resources.CREATENEW_ACTION, "Create", routeValues: null, htmlAttributes: new { @class = "btn btn-primary" })
    </p>
}
<table class="table table-condensed">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SettlementTypeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SettlementTypeDescription)
        </th>
        <th class="hidden-xs">
            @Html.DisplayNameFor(model => model.SettlementTypeShortDescription)
        </th>
        <th class="hidden-xs"></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SettlementTypeId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SettlementTypeDescription)
            </td>
            <td class="hidden-xs">
                @Html.DisplayFor(modelItem => item.SettlementTypeShortDescription)
            </td>
            <td class="hidden-xs btn-group btn-group-xs">
                @if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
                {
                    @Html.ActionLink(Resources.EDIT_ACTION, "Edit", new { id = item.SettlementTypeId }, new { @class = "btn btn-primary" })
                }
                @Html.ActionLink(Resources.DETAILS_ACTION, "Details", new { id = item.SettlementTypeId }, new { @class = "btn btn-info" })
                @if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
                {
                    @Html.ActionLink(Resources.DELETE_ACTION, "Delete", new { id = item.SettlementTypeId }, new { @class = "btn btn-danger" })
                }
            </td>
        </tr>
        <tr class="visible-xs active">
            <td colspan="2" class="btn-group btn-group-xs">
                @if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
                {
                    @Html.ActionLink(Resources.EDIT_ACTION, "Edit", new { id = item.SettlementTypeId }, new { @class = "btn btn-primary" })
                }
                @Html.ActionLink(Resources.DETAILS_ACTION, "Details", new { id = item.SettlementTypeId }, new { @class = "btn btn-info" })
                @if ((ViewBag.IsEcoSystemAdmin) || (ViewBag.IsEnterpriseAdmin))
                {
                    @Html.ActionLink(Resources.DELETE_ACTION, "Delete", new { id = item.SettlementTypeId }, new { @class = "btn btn-danger" })
                }

            </td>
        </tr>


    }

</table>
